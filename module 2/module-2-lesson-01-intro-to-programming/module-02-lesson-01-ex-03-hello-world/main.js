'use strict';
//  así selecciono el elemento con el que quiero trabajar:
const titleElement = document.querySelector('.title');

// así ordeno que el elemento siga igual pero se le añada contenido:
titleElement.innerHTML = titleElement.innerHTML + ' Mundo';